# Port configuration (Render sets PORT automatically)
PORT=3000

# Persistent Storage configuration for Render VPS / Cloud Deployment
# Render Persistent Disk mount path example: /var/data
DATA_DIR=/var/data
UPLOADS_DIR=/var/data/uploads
